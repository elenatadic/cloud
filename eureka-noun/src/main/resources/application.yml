---
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
words: boat,book,vote,seat,backpack,partition,groundhog
server:
  port: ${PORT:${SERVER_PORT:0}} (this will cause a random, unused port to be assigned if none is specified)